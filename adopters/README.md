# Adopters

Adopter-specific mappings belong here and must stay out of the core protocol spec.

Only templates are tracked at the start so the protocol can mature before any one implementation is privileged.
