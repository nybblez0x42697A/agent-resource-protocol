# Models

Formal models and machine-readable definitions belong here.

- `resource-model/`: resource/entity definitions
- `lifecycle-model/`: lifecycle and transition rules
- `lineage-model/`: versioning and rollback structures
- `operator-model/`: evolution operator contracts
- `schemas/`: machine-readable schema artifacts
